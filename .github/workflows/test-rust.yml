name: Rust
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
env:
  CARGO_TERM_COLOR: always
jobs:
  build_and_test:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          components: rustfmt, clippy
      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            dnsraw/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('dnsraw/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-
      - name: Copy blocklist for tests
        run: cp dnsblock.txt dnsraw/dnsblock.txt
      - name: Build
        working-directory: dnsraw
        run: cargo build --verbose
      - name: Run tests
        working-directory: dnsraw
        run: cargo test --verbose
      - name: Lint with Clippy
        working-directory: dnsraw
        run: cargo clippy -- -D warnings
      - name: Check formatting
        working-directory: dnsraw
        run: cargo fmt -- --check
      - name: Install cargo-tarpaulin
        run: cargo install cargo-tarpaulin
      - name: Code coverage
        working-directory: dnsraw
        run: cargo tarpaulin --skip-clean --out Xml
        continue-on-error: true
      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: success() || failure()
        with:
          name: coverage-report
          path: dnsraw/cobertura.xml
